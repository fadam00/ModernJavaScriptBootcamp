//1. You can't define a variable more than once.
let petName = 'Nala';
petName = 'Spot';

console.log(petName);

//2. There are rules related to variable names - Can only use $ and _ as symbol for names

let test_ = 2;
let testy$ = 3;

//3. Variable names cannot be reserved keywords

let let = 12;


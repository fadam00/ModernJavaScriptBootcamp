let age = 28;
let dogYears = (age + 1) / 7

console.log(dogYears);

// Challenge Area

// studentScore = 18;
// maxScore = 20;
// percent = 90;

// Acceptance Criteria: 1) Print the percent.

let studentScore = 15;
let maxScore = 20;
let percent = (studentScore / maxScore) * 100;

console.log(percent);

let isRaining = true;

isRaining = false;

console.log(isRaining);

// the const variable does not allow you to reassign a variable. Best used when you know a variable will never be reassigned.

const person = {
    age: 27
}

person.age = 28 // Valid
// person = {} // Invalid

//Why use const? Code